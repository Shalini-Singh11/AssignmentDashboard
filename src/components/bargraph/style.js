export const bargraphContainer = (darkMode) => ({
    padding: 2,
    paddingY: 2,
    backgroundColor: darkMode ? "#FFFFFF0D" : "#F7F9FB", 
    borderRadius: "16px",
    maxHeight: 270,
    width: "100%", 
    maxWidth: "100%", 
    overflow: "hidden", 
  });
  
  export const bargraphText = (darkMode) => ({
    marginBottom: 2,
    color: darkMode ? "#FFF" : "#000000",
    fontWeight: "bold",
  });
