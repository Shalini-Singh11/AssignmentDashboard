export const tableContainerStyle = (darkMode) => ({
    p: 2,
    backgroundColor: darkMode ? "#FFFFFF0D" : "#F7F9FB", 
    borderRadius: "16px",
    overflowX: "auto"
  });
  
  export const titleStyle = (darkMode) => ({
    fontWeight: 'bold',
    paddingLeft: 2,
    color: darkMode ? 'white' : 'black', 
  });
  
  export const tableHeaderStyle = (darkMode) => ({
    color: darkMode ? "#FFFFFF66" : "#1C1C1C66", 
    fontSize: "14px",
  });
  
  export const tableRowStyle = (darkMode) => ({
    border: 'none',
    color: darkMode ? 'white' : 'black',
  });
  